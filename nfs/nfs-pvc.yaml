apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-persistent-volume-claim
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
